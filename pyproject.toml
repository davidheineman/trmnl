[project]
name = "trmnl"
version = "0.1.0"
description = "python client for TRMNL"
requires-python = ">=3.10"
dependencies = ["requests>=2.28"]

[tool.setuptools]
py-modules = ["trmnl"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
